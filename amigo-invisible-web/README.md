# Amigo Invisible Web

Minimal React + Vite prototype for Secret Santa (Amigo Invisible).

## Setup

1. npm install
2. npm run dev

TypeScript + React + Vite + Tailwind (optional - included in devDependencies).

This prototype runs the draw algorithm entirely on the client.
